#include <stdio.h>
#include <stdlib.h>

#define MAX_SIZE 100

typedef struct {
    int arr[MAX_SIZE];
    int top
} Stack;

void init(Stack *s) {
    s->top = 0;
}

int main(void) {
    Stack s;
    return 0;
}